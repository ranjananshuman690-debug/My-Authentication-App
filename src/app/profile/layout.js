export const metadata = {
  title: "Profile - My Profile",
  description: "Your smart authentication platform powered by modern security",
};

export default function HomeLayout({ children }) {
  return children;
}
