export const metadata = {
  title: "Services - Our Services",
  description: "Everything you need to build a secure authentication system",
};

export default function ServicesLayout({ children }) {
  return children;
}
