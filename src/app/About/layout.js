export const metadata = {
  title: "About - About Us",
  description: "We build secure, modern and scalable web applications",
};

export default function AboutLayout({ children }) {
  return children;
}
